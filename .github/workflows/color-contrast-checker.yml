# File: .github/workflows/color-contrast-checker.yml

name: Run Color Contrast Analysis

on:
  workflow_dispatch:
    inputs:
      foreground_color:
        description: 'Foreground color (hex format)'
        required: true
        type: string
      background_color:
        description: 'Background color (hex format)'
        required: true
        type: string
      session_id:
        description: 'Unique session identifier'
        required: true
        type: string

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # Updated to v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Run Color Contrast Analysis
        id: run_analysis
        env:
          FOREGROUND_COLOR: ${{ github.event.inputs.foreground_color }}
          BACKGROUND_COLOR: ${{ github.event.inputs.background_color }}
          SESSION_ID: ${{ github.event.inputs.session_id }}
        run: |
          # Create a temporary file for output with session ID
          OUTPUT_FILE="color_contrast_results_${SESSION_ID}.json"
          
          # Create a session marker file for easier tracking
          echo "Session ID: $SESSION_ID" > session_marker_${SESSION_ID}.txt
          echo "Foreground: $FOREGROUND_COLOR" >> session_marker_${SESSION_ID}.txt
          echo "Background: $BACKGROUND_COLOR" >> session_marker_${SESSION_ID}.txt
          echo "Timestamp: $(date +%s)" >> session_marker_${SESSION_ID}.txt
          
          # Run the Python script and capture output
          python runner/color_contrast_checker.py > script_output.txt 2>&1 || true
          
          # Try to find the results in the script output
          if grep -q "results=" script_output.txt; then
            # Extract the JSON results
            grep "results=" script_output.txt | sed 's/results=//' > "$OUTPUT_FILE"
            echo "Results extracted to $OUTPUT_FILE"
          else
            # If no results found, create an error response
            echo '{"status": "error", "message": "No results found in script output"}' > "$OUTPUT_FILE"
          fi
          
          # Display the output for debugging
          echo "=== Script Output ==="
          cat script_output.txt
          echo "=== Extracted Results ==="
          cat "$OUTPUT_FILE"
          
          # Save the file path for the next step
          echo "output_file=$OUTPUT_FILE" >> $GITHUB_OUTPUT

      - name: Upload Results as Artifact
        uses: actions/upload-artifact@v4  # Updated to v4
        with:
          name: color-contrast-results-${{ github.event.inputs.session_id }}
          path: ${{ steps.run_analysis.outputs.output_file }}
          retention-days: 1
          
      - name: Upload Session Marker
        uses: actions/upload-artifact@v4
        with:
          name: session-marker-${{ github.event.inputs.session_id }}
          path: session_marker_${{ github.event.inputs.session_id }}.txt
          retention-days: 1
